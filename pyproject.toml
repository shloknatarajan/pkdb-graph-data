[project]
name = "pkdb-graph-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pubmed-markdown>=0.2.5",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
evals = [
    "anthropic>=0.116.0",
    "google-genai>=1.0.0",
    "numpy>=2.4.6",
    "openai>=2.0.0",
    "scipy>=1.17.1",
]
